import PhaseStub from '@/components/PhaseStub';

export const metadata = { title: 'Browse' };

export default function BrowsePage() {
  return (
    <PhaseStub
      title="Browse"
      subtitle="Your words as a table or bento grid, filterable by source, mastery, and due date. Coming in Phase 3."
      phase="Phase 3"
    />
  );
}

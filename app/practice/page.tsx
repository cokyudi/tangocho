import PhaseStub from '@/components/PhaseStub';

export const metadata = { title: 'Practice' };

export default function PracticePage() {
  return (
    <PhaseStub
      title="Practice"
      subtitle="Spaced-repetition flashcards (SM-2). Flip, rate, and let the app schedule your reviews. Coming in Phase 4."
      phase="Phase 4"
    />
  );
}
